# Flat node_modules (like npm/yarn-classic) so the standalone container + the
# `npx tsx` MCP child resolve transitive deps and @cursor/sdk native binaries
# exactly as today. Revisit once the MCP server is bundled / moved to NestJS.
node-linker=hoisted
shamefully-hoist=true
